/** @type {import('next').NextConfig} */
const nextConfig = {
  // Shu yerda lokal IP yoki ruxsat berilishi kerak bo'lgan domenlarni yozasiz
  allowedDevOrigins: ['192.168.1.173', 'localhost:3000'],
};

export default nextConfig;